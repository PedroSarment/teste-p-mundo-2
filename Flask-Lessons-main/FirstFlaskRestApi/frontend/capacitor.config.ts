import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Escolas-pelo-mundo-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
